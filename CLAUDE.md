# PIXBI Lineage Visualizer

Интерактивная визуализация lineage данных для дашбордов PIXBI.
Реализация: кастомная визуализация PIX BI на **ECharts** (`series.type: 'graph'`,
`layout: 'none'` — узлы позиционируются вручную по слоям).

---

## 📁 Структура проекта

```
pixbi_lineage/
├── lineage.js        # Кастомная визуализация PIX BI v1 (legacy, одна категория, 5 слоёв)
├── lineage_v2.js     # Кастомная визуализация PIX BI v2 (несколько блоков, динамические слои)
├── <dataset>.csv     # Исходный датасет (для анализа, в рантайме не используется)
└── CLAUDE.md         # Этот файл
```

---

# lineage_v2.js (актуальная версия)

## Архитектура: один блок, склейка пути через `||`

v2 использует **один блок PIX BI** с **одним вычисляемым полем** `lineage_key`,
в котором весь путь склеен через разделитель слоёв `||`. Каждый сегмент между
`||` = один слой графа (по позиции). В отличие от v1, число слоёв **динамическое**
(определяется числом сегментов), а не захардкожено.

```
lineage_key (одно поле) ──► lineage_v2.js ──► ECharts Graph
  "Схема::v1::инфо || Таблица::v2::инфо || Витрина::v3::инфо || Дашборд::v4::инфо"
       слой 0            слой 1           слой 2            слой 3
```

### Конфигурация в PIX BI

**Блок** (где `lineage_v2.js`):
- **Категории:** ОДНО вычисляемое поле `lineage_key` (формат ниже)
- **Меры:** `COUNT(1)` — обязательно, иначе 0 строк
- **Серии:** пусто
- `BLOCK_ID` в `lineage_v2.js` = ID этого блока (он же DOM-элемент для рендера)

### Формат данных (`lineage_key`)

Каждая строка `categories[i].name` = одна цепочка пути:

```
Название::значение::инфо || Название::значение::инфо || ...
```

- **Разделитель слоёв:** `||` (`LAYER_SEP`). Каждый сегмент = слой по позиции.
- **Внутри сегмента:** `Название::значение::инфо` (`FIELD_SEP` = `::`).

| Часть | Назначение |
|-------|------------|
| `Название` | Метка слоя (легенда). Слой определяется ПОЗИЦИЕЙ, не названием — одинаковые названия на разных слоях допустимы. |
| `значение` | Имя узла (внутри узла и в тултипе). |
| `инфо` | Доп. информация (синим в тултипе). Может быть пустым. |

Связи строятся **внутри строки**: сегмент i → следующий непустой сегмент i+1.
Отдельный «ключ» не нужен. Пустые сегменты (значение `''`) пропускаются — ребро
идёт к ближайшему непустому слою справа. Завершающий `||` (пустой хвостовой слой)
игнорируется.

**Пример SQL (PIX BI вычисляемое поле):**
```sql
concat(
  'Схема в GreenPlum',  '::', coalesce(table_schema,''), '::', concat('Домен СИДа: ', coalesce(sid_domen,'')), '||',
  'Таблицы в GreenPlum','::', coalesce(table_name,''),   '::', concat('Домен СИДа: ', coalesce(sid_domen,'')), '||',
  'Витрины в GreenPlum','::', coalesce(view_schema,''),  '::', concat('Домен СПДа: ', coalesce(spd_domen,'')), '||',
  'Витрины в GreenPlum','::', coalesce(view_name,''),    '::', concat('Домен СПДа: ', coalesce(spd_domen,''))
) as lineage_key
```

### `BLOCK_ID`

В начале `lineage_v2.js`:

```js
const BLOCK_ID = 'id_блока';   // ID блока, он же DOM-элемент для рендера
```

**Число и порядок слоёв** задаётся числом и порядком сегментов в `lineage_key`
(макс. 20 — по размеру палитры). Чтобы добавить/убрать слой — правится SQL-формула,
код менять не нужно.

---

## 🏗 Архитектура графа (v2)

### Слои

Один сегмент `||` = один слой = одна колонка на графе.
Слои идут слева направо в порядке сегментов в `lineage_key`.
Метка слоя берётся из поля `Название` первого непустого сегмента на этой позиции.

### Цвета

Назначаются из палитры 20 цветов по индексу слоя:

| Индекс | Цвет |
|--------|------|
| 0 | `#22c55e` зелёный |
| 1 | `#eab308` жёлтый |
| 2 | `#3b82f6` синий |
| 3 | `#a855f7` фиолетовый |
| 4 | `#ef4444` красный |
| 5 | `#f97316` оранжевый |
| 6 | `#06b6d4` циан |
| 7 | `#ec4899` розовый |
| 8 | `#14b8a6` teal |
| 9 | `#8b5cf6` индиго |
| 10 | `#84cc16` лайм |
| 11 | `#f43f5e` rose |
| 12 | `#0ea5e9` sky |
| 13 | `#d946ef` fuchsia |
| 14 | `#e11d48` crimson |
| 15 | `#65a30d` olive-green |
| 16 | `#0891b2` dark-cyan |
| 17 | `#c026d3` magenta |
| 18 | `#ea580c` burnt-orange |
| 19 | `#2563eb` royal-blue |

**Форма узлов:** все `roundRect` (прямоугольники со скруглёнными углами).

### Связи (Edge)

- Рёбра строятся ВНУТРИ каждой строки `lineage_key`.
- Сегменты строки идут по позиции: слой 0 → слой 1 → ... → слой N-1.
- Соединяется сегмент с ближайшим непустым сегментом справа.
- Если сегмент пустой (значение `''`) — он пропускается, ребро идёт к
  следующему доступному слою.
- Граф направленный (`edgeSymbol: ['none', 'arrow']`).
- Дедупликация через `linkSet` (ключ `src>>tgt`).

### ID узла

```
${layerIndex}::${value}
```

Уникален в пределах слоя. Например: `0::ods_atlas`, `4::Мой дашборд`.

---

## 🧩 Модель данных (v2, в памяти)

### Node (`nodeMap`)
```js
{
  id,            // "layerIndex::value", напр. "0::ods_atlas"
  name,          // значение (отображаемое имя)
  layer,         // индекс слоя (0..N-1)
  layerLabel,    // Название слоя (из данных), напр. "Схема GP"
  details: { _info },
  refCount       // счётчик упоминаний
}
```

### Edge (`linkList`)
```js
{ source: "<node-id-from>", target: "<node-id-to>" }
```

---

## 🎨 Дизайн и поведение (v2)

### Узлы
- `symbol: 'roundRect'`, фиксированный размер `[NODE_W, NODE_H]`
  (`NODE_W = 160`, `NODE_H = 30`) — узлы НЕ сжимаются
- Цвет — `COLOR_PALETTE[layerIndex]`, обводка `#fff`, подпись внутри
- Слои по горизонтали: `x = layerIndex * LAYER_GAP` (`LAYER_GAP = NODE_W + 280`)
- Узлы внутри слоя — вертикально, шаг `NODE_H + V_GAP` (`V_GAP = 24`),
  сортировка по имени

### Размер canvas и обход авто-fit (важно)
ECharts с `layout:'none'` по умолчанию **авто-вписывает** (fit) весь граф в
размер контейнера — при множестве узлов это схлопывает их в кучу. Декларативный
`zoom`/`center` это не лечит (fit перебивает их).

Решение: **canvas делается физически равным размеру графа**.
- Узлы сдвигаются в положительные координаты от `(PAD, PAD)` (`PAD = 80`).
- Вычисляются `logicalW × logicalH` по bbox узлов + поля.
- Внутри блока `el` создаётся вложенный `innerEl` размером `logicalW × logicalH`,
  и ECharts рендерит **на нём**. Авто-fit становится тождественным (вписывать
  граф в canvas его же размера нечего) → масштаб 1, узлы натуральные, зазоры
  держатся.
- `el.style.overflow = 'auto'` — граф крупнее окна прокручивается нативно.
- После рендера `centerScroll()` ставит середину графа по центру видимой области
  (с повтором — PIX BI грузится асинхронно, `clientWidth/Height` могут быть 0).

### Рёбра
- Направленные (`edgeSymbol: ['none', 'arrow']`), `curveness: 0.3`
- По умолчанию `#94a3b8`, при выделении пути — `#60a5fa`

### Взаимодействие
- **Hover / клик** → подсветка полного пути: узел увеличивается, предки —
  синяя обводка, потомки — зелёная, рёбра пути жирные/синие, остальное гаснет.
  Клик закрепляет (`lockedNodeId`), повторный клик / клик по пустому месту — снимает.
- **Двойной клик** → сброс.
- **Тултип**: `_fullName`, `_info` (синим), название слоя + индекс, `refCount`.
- `roam` (zoom/pan) + `draggable`, `scaleLimit` 0.3..4.
- Прокрутка графа внутри блока — нативная (`overflow:auto` на контейнере),
  плюс панорамирование/зум мышью. Дефолтный фокус — центр графа (`centerScroll`).
- `currentView()` читает текущий zoom/center из модели ECharts, чтобы подсветка
  пути (`replaceMerge`) не сбрасывала ручной зум/панораму к стартовому виду.

### Тема
- Адаптивная из `window.themeConfigs`.
- Отслеживание: `MutationObserver` + `postMessage` (`THEME_CHANGED`).

### Диагностика
- Если нет данных — дамп на canvas с подсказками.

---

# lineage.js (legacy v1)

Фиксированная визуализация: **один блок**, один `lineage_key`, 5 хардкодных слоёв,
10 полей с индексами 0–9. Формат поля: `значение::инфо`.
**Сохранена для обратной совместимости. Новые дашборды — `lineage_v2.js`.**

## 📊 Датасет (v1)

**Файл:** `edw_prj_brddwh_v_serv_dashboard_linage_prod_202606031303.csv`
**Строк:** ~15 634
**Разделитель:** `;`

### Поля CSV

| # | Поле | Описание |
|---|------|----------|
| 1 | `ind_name` | Тип подключения дашборда (3 варианта) |
| 2 | `table_schema` | Схема источника (GreenPlum) |
| 3 | `table_name` | Таблица источник |
| 4 | `view_schema` | Схема представления |
| 5 | `view_name` | Имя представления |
| 6 | `adqm_src_table_schema` | База в ADQM (`brd_cds`) |
| 7 | `adqm_src_table_name` | Таблица в ADQM |
| 8 | `adqm_view_schema` | Схема представления ADQM |
| 9 | `adqm_view_name` | Представление ADQM |
| 10 | `app_id` | ID дашборда |
| 11 | `app_name` | Название дашборда |
| 12 | `app_author` | Автор |
| 13 | `dataset_name` | Название датасета |
| 14 | `sid_domen` | Домен (19 шт.) |
| 15 | `spd_domen` | SP-домен |
| 16 | `pix_domen` | PIX-домен |
| 17 | `write_ts` | Время записи |
| 18 | `wf_load_idn` | ID загрузки |

### Формат `lineage_key` (v1)

10 полей, разделены `||`. Каждое поле: `значение::инфо`.
Фиксированные индексы (0–9): srcSchema, srcTable, vSchema, vName, aDb, aTbl,
avSchema, avName, appId, appName.

### Слои (v1, хардкод)

| Слой | Цвет | Типы узлов |
|------|------|-------------|
| Source | 🟢 `#22c55e` | `source_schema`, `source_table` |
| View | 🟡 `#eab308` | `view_schema`, `view` |
| ADQM | 🔵 `#3b82f6` | `adqm_db`, `adqm_table` |
| ADQM View | 🟣 `#a855f7` | `adqm_view_db`, `adqm_view` |
| Dashboard | 🔴 `#ef4444` | `dashboard` |

---

## 🛠 Технический стек (обе версии)

| Компонент | Технология |
|-----------|------------|
| Граф | ECharts `series.type: 'graph'` |
| Layout | `layout: 'none'` — ручное позиционирование |
| Рендер | Canvas размером графа во вложенном `innerEl` (обход авто-fit), `symbol: 'roundRect'` |
| Прокрутка | `overflow:auto` на блоке + `roam`/`draggable`; дефолт-фокус по центру |
| Взаимодействие | `roam`/`draggable` + кастомные `click`/`dblclick` |
| Данные | `getItemData(id)` → `categories[i].name` |
| Тема | `window.themeConfigs` |

---

## 📐 Конвенции

- ECharts, `series.type: 'graph'`, `layout: 'none'`
- Один файл `.js` на версию, без сборщиков
- Язык интерфейса: русский
- Тема адаптивная (PIX BI), **не** хардкод
- **Новые дашборды — `lineage_v2.js`**. `lineage.js` — для старых блоков.
- `BLOCK_IDS` / `const id` — обновлять при пересоздании блоков